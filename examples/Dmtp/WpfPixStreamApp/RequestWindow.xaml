<Window x:Class="WpfApp1.RequestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="请求方 - PixStream 显示" Height="650" Width="950"
        Loaded="Window_Loaded" Closed="Window_Closed">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="10,6">
            <TextBlock x:Name="txtStatus" Text="连接中…" FontSize="13" Margin="0,2"/>
            <TextBlock x:Name="txtFrameInfo" Text="" FontSize="11" Foreground="Gray" Margin="0,2"/>
        </StackPanel>

        <Image Grid.Row="1" x:Name="imgDisplay" Stretch="Fill" Margin="5"/>
    </Grid>
</Window>
