<Project>

	<PropertyGroup>
		<BaseVersion>4.0.0-beta.55</BaseVersion>
	</PropertyGroup>

	<PropertyGroup Condition="'$(Configuration)'=='Release'">
		<TouchSocketVersion>$(BaseVersion)</TouchSocketVersion>
	</PropertyGroup>
	<PropertyGroup Condition="'$(Configuration)'=='Debug'">
		<TouchSocketVersion>$(BaseVersion)</TouchSocketVersion>
	</PropertyGroup>

</Project>